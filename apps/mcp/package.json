{
  "name": "@deploylite/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --allowOnly=false",
    "test:only-guard": "node ./scripts/assert-vitest-only-guard.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@deploylite/config": "workspace:*",
    "@deploylite/contracts": "workspace:*",
    "zod": "^3.24.1"
  }
}
