{
  "name": "@deploylite/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --allowOnly=false --config vitest.config.ts",
    "db:generate": "drizzle-kit generate --config drizzle.config.ts",
    "db:migrate": "node scripts/migrate.mjs",
    "db:check": "node scripts/check.mjs",
    "db:verify:integration": "DEPLOYLITE_DB_INTEGRATION=1 vitest run --allowOnly=false --config vitest.config.ts src/postgres.integration.test.ts"
  },
  "dependencies": {
    "@deploylite/config": "workspace:*",
    "@deploylite/contracts": "workspace:*",
    "@deploylite/domain": "workspace:*",
    "bcryptjs": "^3.0.3",
    "drizzle-orm": "^0.45.2",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "drizzle-kit": "^0.30.2"
  }
}
